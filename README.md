# Glass Bridge

Glass Bridge is a visual instrument for VCV Rack.

It transforms CV signals into evolving particles, lines, and structures.

## Build

export RACK_DIR="$HOME/Documents/Rack-SDK"
make clean
make dist

## Install

make install
